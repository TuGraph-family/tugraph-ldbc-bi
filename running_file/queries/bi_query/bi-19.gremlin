g.V($city1Id).in('isLocatedIn').hasLabel('Person').shortestPath()
                    .with(ShortestPath.target, __.out('isLocatedIn').hasId($city2Id))
                    .with(ShortestPath.maxDistance, 10000)
                    .with(ShortestPath.edges, bothE('weight19'))
                    .with(ShortestPath .includeStartId, true)
                    .with(ShortestPath .includeTargetId, true)
                    .with(ShortestPath.distance, '__.values('weight').math('max(round(sqrt(_ / 10)),1)')')
                    .project('person1.id', 'person2.id','totalWeight')
                    .by(values('sourceId')).by(values('targetId')).by(values('weight'))
                    .order().by('person1.id',asc).by('person2.id',asc)